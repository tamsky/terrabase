# ignore all per-directory state
# state should always be stored in s3
.terraform/*

# ignore local backups
*.tfstate.backup

# ignore timestamped backups
terraform.tfstate.*.backup

# ignore saved-plans
saved-plan
saved-plan.*

# generated by our Makefile
environment.sh
terragrunt-default-config.tfvars
terragrunt-local-config.tfvars

# Crash logs are large
**/crash.log
